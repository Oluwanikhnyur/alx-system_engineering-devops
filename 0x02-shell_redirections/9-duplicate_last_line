if [ -e "iacta" ]; then
> last_line=$(tail -n 1 iacta)
> echo "$last_line" >> iacta
>   echo "Duplicated the last line of 'iacta':"
>   echo "$last_line"
> else
>   echo "Error: 'iacta' file not found."
> fi
